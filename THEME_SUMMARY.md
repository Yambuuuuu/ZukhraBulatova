# Темная тема - Краткая инструкция

## ✅ Что сделано

Темы поменяны местами:
- **Темная тема** - по умолчанию (загружается сразу)
- **Светлая тема** - включается кнопкой в header

## 🎨 Как это работает

1. Сайт загружается с темной темой
2. В header справа есть кнопка с иконкой солнца ☀️
3. При клике переключается на светлую тему (иконка меняется на луну 🌙)
4. Выбор сохраняется в браузере

## 📁 Созданные файлы

- `stores/theme.js` - управление темой
- `components/UI/ThemeToggle.vue` - кнопка переключения
- `styles/variables.styl` - обновлены цвета (темная по умолчанию)
- `app.vue` - инициализация темы
- `components/UI/Header.vue` - добавлена кнопка

## 🚀 Запуск проекта

Если у вас проблемы с PowerShell, используйте:
```bash
npx nuxt dev
```

Или создайте файл `start.bat`:
```bat
@echo off
npx nuxt dev
```

## 🎯 Использование в коде

Просто используйте CSS переменные:
```stylus
.my-element
  background var(--background)
  color var(--text-primary)
```

Переменные автоматически меняются при переключении темы!
